#ifndef __Settingsdef__ // Check if Game struct is not defined
#define __Settingsdef__ // Define it to prevent redefinition
typedef struct GlobalGameSettings{
    int ai;
    int hints;
    int gamemode;
    int slow;
} GlGameSettings;
#endif // __GAME_DEFINED__